# ============================================================
# NHIS 다재약물 DDI 웹앱 (hana_app) 전용 패키지
# Mac/Windows 공통 — pip install -r hana_app/requirements.txt
# Python 3.12+ 호환
#
# dev/prod 패리티: ../constraints-py312.txt 가 정확 핀 (Codex 2026-05-06).
# 사용:
#   pip install --constraint constraints-py312.txt -r hana_app/requirements.txt
# ============================================================

# 웹 앱
streamlit>=1.28.0
altair>=5.0.0
watchdog>=3.0.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.26.0,<2.5.0       # 2.5+ 미검증 — constraints-py312.txt 가 정확 핀
pyarrow>=14.0.0,<24.0.0
openpyxl>=3.1.0
pyreadstat>=1.2.6          # SAS 파일 (.sas7bdat/.xpt) 지원, py312 wheel from 1.2.6

# 머신러닝 — XGBoost 세그폴트 회귀 방지 위해 constraints-py312.txt 가 정확 핀
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.3.0
lightgbm>=4.0.0,<5.0.0
catboost>=1.2.5             # py312 wheel from 1.2.5
shap>=0.44.0
joblib>=1.3.0

# 수치/통계
scipy>=1.10.0
statsmodels>=0.14.0

# 시각화
matplotlib>=3.7.0
plotly>=5.0.0

# 유틸리티
duckdb>=1.0.0
PyYAML>=6.0.0
python-dotenv>=1.0.0
requests>=2.31.0
loguru>=0.7.0

# SAP HANA 연결 (선택사항 — 없어도 SAS 파일 모드로 작동)
hdbcli>=2.18.22

# 보안 — DB 비밀번호 OS Keychain 저장
keyring>=24.0.0

# 시스템 모니터링 (page 3 RAM 감지용 — 없으면 기본값 8192MB 사용)
psutil>=5.9.0
