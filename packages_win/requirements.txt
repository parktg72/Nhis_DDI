# ============================================================
# DDI 모델 - 공통 패키지 요구사항 (Python 3.12, Windows x64)
# 폐쇄망 오프라인 설치용 / Python 3.12+ 호환
# ============================================================

# -- XML 처리
lxml>=4.9.0,<6.0.0

# -- 데이터 처리
pandas>=2.1.0,<3.0.0
numpy>=1.26.0
pyarrow>=14.0.0
duckdb>=1.0.0

# -- HTTP / API
requests>=2.31.0
aiohttp>=3.9.0
httpx>=0.25.0

# -- 설정 / 유틸
PyYAML>=6.0.0
python-dotenv>=1.0.0
pydantic>=2.4.0
tqdm>=4.66.0
loguru>=0.7.0
click>=8.1.0
psutil>=5.9.0

# -- 머신러닝 Phase 2
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0
shap>=0.43.0
joblib>=1.3.0

# -- 머신러닝 Phase 3 (딥러닝)
torch>=2.2.0
pytorch-tabnet>=4.0.0

# -- 웹 앱 (Streamlit)
streamlit>=1.30.0
plotly>=5.18.0

# -- API 서빙
fastapi>=0.104.0
uvicorn>=0.24.0

# -- 데이터 검증
great-expectations>=0.18.0

# -- 모니터링
prometheus-client>=0.18.0

# -- 테스트
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# ============================================================
# 폐쇄망 설치 (Python 3.12):
#   pip install --no-index --find-links=packages_win/py312 -r packages_win/requirements.txt
# 또는 통합 설치:
#   install_312.bat venv
# ============================================================
